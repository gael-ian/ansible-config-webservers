# {{ ansible_managed }}


check file php_fpm_{{ php_fpm_pool.name }}_log_access with path /var/log/php/{{ php_fpm_pool.name }}/access.log
  group php

  if size > 250 MB then exec "/usr/sbin/logrotate -f /etc/logrotate.d/php"


check file php_fpm_{{ php_fpm_pool.name }}_log_error with path /var/log/php/{{ php_fpm_pool.name }}/error.log
  group php

  if size > 250 MB then exec "/usr/sbin/logrotate -f /etc/logrotate.d/php"


check file php_fpm_{{ php_fpm_pool.name }}_log_slow with path /var/log/php/{{ php_fpm_pool.name }}/slow.log
  group php

  if size > 250 MB then exec "/usr/sbin/logrotate -f /etc/logrotate.d/php"
