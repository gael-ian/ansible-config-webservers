---
- name: "Configure Monit for {{ monit_servive.name }}"
  template:
    src: "{{ monit_servive.template }}"
    dest: "/etc/monit/services.d/{{ monit_servive.name }}"
    owner: root
    group: root
    mode: 0644
  notify:
    - reload monit
