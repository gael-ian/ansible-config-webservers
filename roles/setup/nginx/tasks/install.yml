---
- name: Ensure nginx is installed.
  apt:
    name: "{{ item }}"
    state: installed
  with_items:
    - nginx
    - nginx-extras

- name: Ensure nginx is started and enabled to start at boot.
  service:
    name: nginx
    state: started
    enabled: yes
