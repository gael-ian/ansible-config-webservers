---
- name: "Configure Monit for {{ service.value.name }}"
  template:
    src: "{{ service.value.template }}"
    dest: "/etc/monit/services.d/{{ service.value.name }}"
    owner: root
    group: root
    mode: 0644
  notify:
    - reload monit
